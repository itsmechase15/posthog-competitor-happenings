# One announcement URL. Changing this file on main runs
# .github/workflows/force-post.yml, which posts that item to Slack even if the
# dedupe table has seen it before. Lines starting with # are ignored, so
# touching a comment is enough to ask for the same URL to be posted again.
# retrigger 2026-09-08 to see the linked product name in the action title
# retrigger 2026-09-08 now that a recommend is checked against the PostHog docs:
# this one should read as a gap in Experiments next to scheduled flag changes,
# not as "PostHog cannot schedule anything"
# retrigger 2026-09-08 again: the last attempt reached the model, read the docs,
# and then lost the reply to a blank "feature" field, so Slack got the heuristic
# summary instead of the recommend
# retrigger 2026-09-08 for the first post that opens one issue per recommended
# action: each stacked action should carry its own "Access GitHub issue #n"
# retrigger 2026-09-08 for the first post where each action leads with the work:
# consider enhancing should open with the scheduled end time to add, and update
# pages should name the page and the line to change on it
# retrigger 2026-09-08 now that the detail line has room for a whole
# lead-with-the-work sentence: the last post cut both of them mid-word at 150
# characters ("experimen…", "ships tod…")
https://amplitude.com/releases/schedule-experiment-stop
