# One announcement URL. Changing this file on main runs
# .github/workflows/force-post.yml, which posts that item to Slack even if the
# dedupe table has seen it before. Lines starting with # are ignored, so
# touching a comment is enough to ask for the same URL to be posted again.
# retrigger 2026-09-08 to see the linked product name in the action title
# retrigger 2026-09-08 now that a recommend is checked against the PostHog docs:
# this one should read as a gap in Experiments next to scheduled flag changes,
# not as "PostHog cannot schedule anything"
# retrigger 2026-09-08 again: the last attempt reached the model, read the docs,
# and then lost the reply to a blank "feature" field, so Slack got the heuristic
# summary instead of the recommend
# retrigger 2026-09-08 for the first post that opens one issue per recommended
# action: each stacked action should carry its own "Access GitHub issue #n"
# retrigger 2026-09-09 now that a page edit has to be about the launch that
# found it: this one should carry no update_pages about basic A/B testing, and
# most likely no update_pages at all
# retrigger 2026-09-08 for the first post where each action leads with the work:
# consider enhancing should open with the scheduled end time to add, and update
# pages should name the page and the line to change on it
# retrigger 2026-09-08 now that the detail line has room for a whole
# lead-with-the-work sentence: the last post cut both of them mid-word at 150
# characters ("experimen…", "ships tod…")
# retrigger 2026-09-09 to confirm the topic guard holds on a second run: the
# alert should lead Experiments with the scheduled end time and stay free of an
# update_pages about basic A/B testing or holdouts
# retrigger 2026-09-09 for the first post carrying the whole impact scale in the
# issue body: ## Impact should list minor, notable and major, with the checked
# box on this alert's level instead of printing that one label alone
# 2026-09-09 switching to Mixpanel to see the impact scale on a second post.
# Every entry in Mixpanel's changelog feed carries the same link, so this URL
# names the feed rather than one item: it resolves to whichever entry is newest,
# today "First-Party Domains" from 2026-08-27.
# 2026-09-09 back to Amplitude for the first post carrying the alert break:
# the message should open with a divider and an "Amplitude · ..." header above
# the image, and each issue should name its related team(s).
# 2026-09-09 and one Mixpanel changelog item straight after, so the divider and
# header can be read where they matter: two alerts in a row that Slack would
# otherwise collapse into one another.
# 2026-09-09 the same Mixpanel changelog item again, now that the catalog knows
# about the reverse proxy. Today's newest entry is First-Party Domains, which is
# the launch that got this wrong: the run should put
# posthog.com/docs/advanced/proxy in front of the model, the alert should read
# as a gap next to PostHog's managed reverse proxy rather than something PostHog
# has never shipped, and the enhance issue should carry "Docs that would change
# if this ships" listing those proxy pages. No update_pages should point at a
# docs URL.
# 2026-09-09 First-Party Domains once more, now named by its own anchor rather
# than by the page every Mixpanel entry shares. The alert should open with a
# screenshot of that entry instead of the "Changelogs" card, and the KNOW
# sentence should end with a linked changelog pointing at the same anchor.
# 2026-09-09 back to the Amplitude scheduled experiment stop for the first post
# routed to PostHog's real small teams: the Related team(s) line on the enhance
# issue should read Experiments, not Product.
# 2026-09-09 and Mixpanel's First-Party Domains straight after, because it is
# the other half of the same proof: a launch about ingestion plumbing should
# name Ingestion, and a page edit about it should name Marketing, where the old
# map could only ever have said "Product, Engineering".
# 2026-09-13 the first post under the new impact rules, which rate a post on
# what it shipped. Amplitude's scheduled experiment stop is a new control on
# Experiments, a product they already had, so Slack should read Notable where
# the old "cosmetic or incremental" wording had it at Minor. It did.
# 2026-09-13 and Mixpanel's First-Party Domains straight after, as the other
# half of the proof: serving events through a customer's own domain is a
# capability Mixpanel did not have before, so this one should read Major.
# 2026-09-13 the MXP London panel recap, which went out as (post) and is the
# reason the source tag changed. A page on mixpanel.com is an article, so the
# KNOW line should end with a linked (article) and the footer should read
# "Mixpanel · article". Impact stays Minor: the recap ships no feature.
https://mixpanel.com/blog/ai-in-product-development
